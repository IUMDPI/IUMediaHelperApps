<UserControl x:Class="Recorder.Controls.RecordPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Recorder.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Visibility="{Binding Visibility}">
    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <Button FontFamily="Webdings" Cursor="Hand" Foreground="{Binding RecordButtonForeground}" Background="{Binding RecordButtonBackground}" FontSize="64" Width="72" Height="72" Margin="12,12,12,12" Command="{Binding RecordCommand}">
                <TextBlock Text="{Binding RecordButtonLabel}"></TextBlock>
            </Button>
            <Label HorizontalContentAlignment="Center"><TextBlock Text="{Binding RecordCaption}" /></Label>
        </StackPanel>
    </Grid>
</UserControl>
